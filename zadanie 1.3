#include <iostream>

using namespace std;

int main()
{
    int number;

    cout << "Podaj liczbe calkowita: ";
    cin >> number;

    if (number > 0) {
        cout << "Wpisales dodatnia liczbe calkowita: " << number << endl;
    }
    else if (number < 0) {
        cout << "Wpisales ujemna liczbe calkowita: " << number << endl;
    }
    else { // Użyj 'else' bez warunku dla zera
        cout << "Wpisales liczbe o wartosci: " << number << endl;
    }

    return 0;
}
